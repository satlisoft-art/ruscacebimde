name: Build Play Store Release AAB with Logo

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          channel: 'stable'
          cache: true

      - name: Install Dependencies
        run: flutter pub get

      - name: Add Launcher Icons Dependency
        run: |
          echo "dev_dependencies:" >> pubspec.yaml
          echo "  flutter_launcher_icons: \"^0.13.1\"" >> pubspec.yaml
          echo "" >> pubspec.yaml
          echo "flutter_launcher_icons:" >> pubspec.yaml
          echo "  image_path: \"logo.png\"" >> pubspec.yaml
          echo "  android: true" >> pubspec.yaml
          echo "  ios: true" >> pubspec.yaml
          flutter pub get

      - name: Generate App Icon
        run: flutter pub run flutter_launcher_icons:main

      - name: Build App Bundle (AAB)
        run: flutter build appbundle --release

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-aab
          path: build/app/outputs/bundle/release/app-release.aab
